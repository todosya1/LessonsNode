🚀 Middleware в Express.js - Простое объяснение

1. Что такое Middleware?
───────────────────────
Middleware - это функции-посредники, которые выполняются между получением запроса (request) 
и отправкой ответа (response). Представьте их как "охранников", которые проверяют и могут 
изменять данные, проходящие через сервер.

2. Основные возможности API
───────────────────────────
GET /products         → Получить список всех продуктов
GET /products/:id     → Найти продукт по ID
DELETE /products/:id  → Удалить продукт
POST /products        → Создать новый продукт

3. Как работает Middleware?
──────────────────────────
Request → Middleware1 → Middleware2 → Response

Пример простого middleware:
```javascript
app.use((req, res, next) => {
    console.log('Запрос получен в:', new Date());
    next();  // Передаем управление дальше
});
```

4. Важные особенности
────────────────────
• Middleware всегда имеет доступ к объектам req (запрос) и res (ответ)
• Функция next() обязательна для передачи управления следующему middleware
• Middleware выполняются в том порядке, в котором они объявлены
• Если не вызвать next(), запрос "застрянет" и клиент не получит ответ

5. Примеры использования
───────────────────────
✓ Логирование запросов
✓ Проверка авторизации
✓ Обработка ошибок
✓ Парсинг данных (например, JSON)
✓ Проверка параметров запроса

6. Пример работы с данными
─────────────────────────
Исходные данные:
• Laptop  - 1500₽
• Phone   - 800₽
• Tablet  - 600₽

Middleware может проверять цены, фильтровать товары, добавлять дополнительную информацию 
перед отправкой клиенту.

💡 Полезный совет: Всегда располагайте middleware в правильном порядке. Например, сначала 
парсинг данных, потом проверка авторизации, затем бизнес-логика.