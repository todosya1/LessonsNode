// Middleware функция для авторизации
// req - объект запроса
// res - объект ответа
// next - функция для передачи управления следующему middleware
const authorize = (req, res, next) => {
    // Выводим сообщение в консоль для отслеживания прохождения авторизации
    // Эмодзи ключа добавлено для визуального выделения в логах
    console.log(' Authorization middleware:');
    
    // Передаем управление следующему middleware
    // В реальном приложении здесь была бы проверка токена авторизации,
    // прав доступа пользователя и т.д.
    next();
};

// Экспортируем функцию authorize для использования в других файлах
module.exports = authorize;